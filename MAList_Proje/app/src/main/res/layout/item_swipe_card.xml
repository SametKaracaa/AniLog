<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardCornerRadius="24dp"
    app:cardElevation="8dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@color/bg_card">

    <ImageView
        android:id="@+id/ivSwipeImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:src="@drawable/shimmer_placeholder" />

    <!-- Gradient overlay for text readability -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_overlay" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:id="@+id/tvSwipeTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="28sp"
            android:textStyle="bold"
            android:shadowColor="#80000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="4"
            tools:text="Jujutsu Kaisen" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/btn_star_big_on"
                app:tint="@color/score_text" />

            <TextView
                android:id="@+id/tvSwipeScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="8.5" />

            <View
                android:layout_width="4dp"
                android:layout_height="4dp"
                android:layout_marginHorizontal="12dp"
                android:background="@color/text_tertiary" />

            <TextView
                android:id="@+id/tvSwipeType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_secondary"
                android:textSize="16sp"
                tools:text="TV" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvSwipeGenres"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/accent_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="Action, Supernatural" />

    </LinearLayout>

    <!-- Overlay indicators for swipe direction -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        
        <!-- Left overlay (Skip) -->
        <TextView
            android:id="@+id/left_overlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="40dp"
            android:layout_marginEnd="40dp"
            android:text="SKIP"
            android:textColor="#EF4444"
            android:textSize="40sp"
            android:textStyle="bold"
            android:rotation="15"
            android:alpha="0"
            android:background="@drawable/chip_bg"
            android:backgroundTint="#33EF4444"
            android:paddingHorizontal="16dp" />

        <!-- Right overlay (Plan to Watch) -->
        <TextView
            android:id="@+id/right_overlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginTop="40dp"
            android:layout_marginStart="40dp"
            android:text="ADD"
            android:textColor="#10B981"
            android:textSize="40sp"
            android:textStyle="bold"
            android:rotation="-15"
            android:alpha="0"
            android:background="@drawable/chip_bg"
            android:backgroundTint="#3310B981"
            android:paddingHorizontal="16dp" />
    </FrameLayout>

</com.google.android.material.card.MaterialCardView>
